
env
XHH_SEARCH_LIMIT=10
XHH_COMMENT_LIMIT=100


含义：

- `XHH_SEARCH_LIMIT`：默认搜索抓多少条帖子
- `XHH_COMMENT_LIMIT`：每个帖子最多写多少条评论到 Excel


python xhh_api.py --search "暗影成双" limit=20


指定每帖评论数量：


python xhh_api.py --search "暗影成双" comment_limit=0


同时指定搜索数量和评论数量：


python xhh_api.py --search "暗影成双" limit=20 comment_limit=0


情投一盒社区帖子爬取
  # 爬取情投一盒社区热门帖子，默认100条
  python topic_feeds_crawler.py --topic-id 416158 --sort hot --limit 100

  # 爬取最新帖子，保存为JSON
  python topic_feeds_crawler.py -t 416158 -s new -l 50 --format json

  # 同时保存Excel和JSON
  python topic_feeds_crawler.py -t 416158 -l 100 --format both -o my_output


## 输出结果

搜索结果会直接保存为 `xlsx`，输出目录在：

[output](/Users/zed/all%20code/D%20互影/media-crawler/xhhBackCrack-main/output)

文件名类似：


general_search_v1_2026018_10712.xlsx


## 表格字段

表格会包含这些列：

- 平台
- 网站链接
- 发布时间
- 数据
- 用户昵称
- 具体内容
- 帖子详情
- 评论
- 作品类型
- 作品标题
- 点赞数量
- 评论数量
- 分享数量
- 用户ID
- 帖子ID
- 标签
- 更新时间
- 是否视频

## 查看帮助


python xhh_api.py --help

